// Check react testing library

import Map from './Map'

describe('Map', () => {
    describe('double click on map', () => {
        it('opens a POI modal', () => {
            const mockDoubleClick = jest.fn();
        })
    })
})
